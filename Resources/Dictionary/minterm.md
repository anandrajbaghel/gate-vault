### Minterm
*Digital Logic Design*

A standard product (AND) term in Boolean algebra where each variable appears exactly once (either complemented or uncomplemented), representing a unique input combination that yields output 1.